export interface Message {
  id: number;
  content: string;
  author: string;
}

export interface Messag {
  id: string;
  message: string;
  sender_id: string;
  receiver_id:string;
  createdAt:string;
}
