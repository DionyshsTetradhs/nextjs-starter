declare module 'pusher-js' {
  class Pusher {
    constructor(apiKey: string, options?: PusherOptions);
    // Declare other methods and properties here
  }
  // Declare other Pusher related types if needed
  export = Pusher;
}
